# SQL reference / archive

Scripts in this folder are **not** run automatically by `node-pg-migrate` or the app. They are kept for documentation, one-off fixes, or historical schema notes. Active migrations live in `backend/migrations/`; canonical schema snapshots in `backend/schema/`.
