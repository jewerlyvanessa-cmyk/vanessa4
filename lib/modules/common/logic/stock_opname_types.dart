/// Tampilan daftar item pada halaman stok opname.
enum StockOpnameListView { pending, verified, missing, all }

/// Aksi setelah opname berhasil disimpan.
enum StockOpnamePostSaveAction { print, viewMissing, newSession, close }
